name: BLS bulk connectivity probe

on:
  push:
    branches:
      - agent/bls-worker-probe
    paths:
      - ".github/workflows/bls-worker-probe.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: Backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install probe dependency
        run: python -m pip install requests
      - name: Run read-only BLS official bulk probe
        run: python -m fundamentals.bls_bulk_probe --from 2026-06-01 --to 2026-06-30 --reference-period 2026-05
